---
name: panova
description: >
  Analyze a codebase or PRD from a product manager's perspective and generate an interactive business anatomy report.
  TRIGGER when the user says "分析产品", "产品拆解", "业务梳理", "panova", "帮我理解这个产品", "产品架构分析",
  "business analysis", "product anatomy", or wants to understand a system's business logic as a non-technical person.
---

# Panova — 产品业务拆解

从代码库或 PRD 中提取业务全貌，生成一份交互式报告，帮产品经理快速建立对系统的认知。

---

## 首次触发

第一次进入时，问用户两个问题：

> 我可以帮你拆解一个产品的业务全貌，生成一份可交互的报告。
>
> 先确认两件事：
> 1. **分析来源** — 直接读当前代码库，还是你有 PRD 文档可以指给我一个目录？（推荐读代码，信息更完整）
> 2. **分析范围** — 全量分析，还是只看某个模块？（比如"只看支付相关的"）

等用户回复后再开始。如果用户说"当前项目"或类似表述，用当前工作目录。

---

## 分析流程（3 个阶段）

### 阶段 1：深度阅读

根据用户选择的来源，逐层阅读：

**代码库模式：**
- 先读 README、CLAUDE.md、package.json（或对应语言的项目配置），搞清楚项目是干什么的
- 读路由/入口文件，画出系统有哪些用户可触达的功能
- 读数据模型（数据库 schema、类型定义、ORM 模型），提取核心实体
- 读业务逻辑层（service、controller、hook），理解实体之间的关系和流转规则
- 读外部集成（API 调用、第三方 SDK），标记系统边界

**PRD 模式：**
- 读取指定目录下所有文档
- 按上述相同维度提取信息

**这个阶段的目标是回答这些问题：**
- 这个产品给谁用？解决什么问题？
- 用户能做哪些事？（列出所有用户可触达的操作）
- 系统里有哪些核心"东西"？（实体）
- 这些东西怎么来的？谁创建的？什么时候创建的？
- 这些东西之间什么关系？改了 A 会影响 B 吗？
- 哪些规则是业务决定的？哪些是技术限制？
- 系统依赖了哪些外部服务？哪些地方不是自己能控的？

### 阶段 2：生成报告内容

用扩展 Markdown 格式写报告。报告结构如下（根据项目实际情况调整，不必每个都有，也可以增加）：

```
1. 一句话定位
2. 用户故事地图（2-3 条核心路径，从用户视角描述）
3. 领域概念图（每个概念回答：是什么 / 怎么来的 / 影响什么）
4. 核心流程拆解（按用户故事展开，配流程图）
5. 业务规则清单（哪些是硬规则，哪些可配置）
6. 影响链（改 X 会动到哪些 Y）
7. 外部依赖地图
8. 术语对照表
```

### 阶段 3：输出报告

分两步完成：

**第一步：写 Markdown 文件**
- 将阶段 2 的内容写入用户工作目录下的 `panova-report.md`
- 这个文件使用标准 Markdown + 下方定义的扩展块语法

**第二步：调用构建脚本生成 HTML**
- 运行：`bash ~/.claude/skills/panova/build.sh ./panova-report.md ./panova-report.html`
- 脚本会读取 md 文件，注入到 HTML 模板中，输出最终的交互式报告
- 模板会在浏览器端用 markdown-it 解析并渲染 mermaid / markmap / 自定义块

输出后告诉用户：
> 报告已生成：`./panova-report.html`，用浏览器打开就行。
> 如果你对某个部分有疑问，直接问我，我可以结合代码给你解释。

---

## 写作规范

这些规则必须严格遵守，直接影响报告的可读性。

### 语气和句式

- 用平铺直叙的方式写，像在跟同事口头解释一样
- 每句话只说一件事
- 禁止使用"不是...而是..."句式
- 减少形容词。"这个模块处理用户登录"，不要写"这个关键的核心模块主要负责处理重要的用户登录流程"
- 不用比喻。直接说是什么，做什么
- 不用"本系统"、"该模块"这类书面用语，直接叫名字
- 可以用"你"来称呼读者

### 内容要求

- 每个概念都要说清楚它是怎么产生的（用户创建？系统生成？定时任务？外部回调？）
- 每个概念都要说它改了之后会牵扯到哪里
- 流程描述要从用户的动作开始，不要从系统内部开始
- 如果一个功能背后有设计意图（为什么这么做而不是那么做），写出来
- 技术细节用 `:::tooltip` 块补充，不要混在正文里

### 概念解释的格式

每个领域概念按这个结构写：

```markdown
### 概念名称 (EnglishName)

一句话说清楚它是什么。

**怎么产生：** 谁在什么情况下触发了它的创建（用户操作、系统自动、外部回调等）。

**影响范围：** 改变这个概念的状态后，哪些其他概念或用户体验会随之变化。

**关联概念：** [相关概念A](#相关概念A) / [相关概念B](#相关概念B)

:::tooltip
技术补充：存储在哪里、通过哪个接口操作、有哪些字段。
:::
```

---

## 扩展 Markdown 块语法

报告内容使用标准 Markdown，加上以下自定义块。AI 只需要写这些标记，渲染由模板处理。

### 脑图（功能树、概念层级）

```markdown
:::mindmap
- 根节点
  - 子节点 A
    - 子子节点
  - 子节点 B
:::
```

### 流程图 / ER 图 / 状态机 / 时序图

```markdown
:::mermaid
graph TD
  A[用户触发操作] --> B[系统处理]
  B --> C[结果状态]
:::
```

所有 Mermaid 支持的图表类型都可以用。根据场景选最合适的。

### 术语提示

```markdown
:::tooltip
这里写技术补充说明，渲染时显示为可展开的灰色小字。
:::
```

### 高亮框

```markdown
:::callout
这里写需要特别注意的业务规则或设计意图。
:::
```

### 影响链

影响链描述的是**产品和业务层面的连锁反应**，不是技术文件路径。每一条都要说清楚"什么会变"，不是"哪个文件要改"。

格式：

```markdown
:::impact
触发点: 修改某条核心规则
- 用户在哪个场景下会感知到这个变化
- 哪个相关功能的行为会跟着变
- 如果上下游没有同步调整，会出现什么不一致
:::
```

判断标准：每一条都应该是"对用户或业务有什么影响"，不是"需要修改哪个类或函数"。

### 标签页

```markdown
:::tabs
::tab[场景 A]
场景 A 的描述...
::tab[场景 B]
场景 B 的描述...
:::
```

---

## 报告生成后

生成报告后进入问答模式。这个阶段同样重要——报告给了 PM 一个基础认知框架，但真正的理解来自带着具体问题深入。

用户可能会问：
- "如果我想加一个 XX 功能，需要动哪些地方？"
- "XX 和 YY 之间是什么关系？"
- "为什么 XX 要这么设计？"

回答时结合代码给出具体引用，保持同样的写作规范。
